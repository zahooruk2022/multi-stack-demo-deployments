---
applications:
  - name: msda-spring-boot-demo-green
    memory: 1G
    instances: 2
    buildpacks:
      - java_buildpack_offline
    path: target/spring-boot-demo-1.0.0.jar
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 21.+ } }'
      APP_VERSION: "2.0.0"
      APP_DEPLOYMENT_COLOR: "green"
    health-check-type: http
    health-check-http-endpoint: /actuator/health
    timeout: 180
    routes:
      - route: msda-spring-boot-demo-green.apps.dhaka.cf-app.com
